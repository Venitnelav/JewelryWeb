@model IEnumerable<JewelryStore.Models.EResin>

@{
    ViewData["Title"] = "Index";
}

<h1>Епоскидна смола</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cost)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InStock)
            </th>
            <th>
                Provisioner
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cost)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InStock)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.provisioner.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.erID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.erID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.erID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
